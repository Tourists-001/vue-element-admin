// 设置
module.exports = {
  title: 'vue-admin-template',

  /**
   * @type {boolean} true || false
   * 是否在侧边栏显示设置
   */
  showSettings: true,

  /**
   * @type {boolean} true || false
   * 是否以标签的形式显示打开过的标签
   */
  tagsView: true,

  /**
   * @type {boolean} true || false
   * 固定导航栏
   */
  fixedHeader: true,

  /**
   * 是否在侧边栏显示logo
   */
  sidebarLogo: true,

  /**
   * @type {String} 'production' | ['production', 'development']
   * 在那个环境中展示错误日志
   */
  errorLog: ['production', 'development']
}
